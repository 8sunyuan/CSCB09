#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
	FILE *fp;
	char *line;
	line = malloc(1000*sizeof(char));
	if ((fp = fopen(argv[1]) == NULL);
		return 1;
	while ((line = fgets(fp)))





}